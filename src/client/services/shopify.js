class Shopify {
    constructor() {}

    async listProducts() {
        // const url = this.base_url + '/admin/api/2020-01/products.json';
        // let products = await fetch(url, { headers: this.headers });
        // console.log('products', products);
    }
}

export default new Shopify();
